def process_user_query(query_string):
    name_list = query_string.split(' ')
    greetings = []
    for name in name_list:
        if (name_list[0].isupper()==True):
            finname.append(name_list)
        greetings.append(f'Hi {finnamne}!')
    return greetings
